<%= form_for(help_page) do |f| %>
  <% if help_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(help_page.errors.count, "error") %> prohibited this help_page from being saved:</h2>

      <ul>
      <% help_page.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit :create, class:"btn btn-default"%>
  </div>
<% end %>